import 'package:flutter/material.dart';

class PrayerScreen extends StatelessWidget {
  static String routeName = '/prayer';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prayer Screen"),
      ),
    );
  }
}
