import 'package:flutter/material.dart';

class ChannelsScreen extends StatelessWidget {
  static String routeName = '/channels';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Channels Screen"),
      ),
    );
  }
}
